import express from 'express'; import path from 'path'; import { fileURLToPath } from 'url'; import fetch from 'node-fetch'; const app = express(); const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); app.use(express.static(path.join(__dirname,'public'))); app.get('*',(req,res)=>res.sendFile(path.join(__dirname,'public','index.html'))); const PORT=process.env.PORT||3000; app.listen(PORT,()=>console.log(`Server running on http://0.0.0.0:${PORT}`)); setInterval(async()=>{try{const r=await fetch('https://afridigital-hub.onrender.com'); console.log(`🌐 Keep-alive ping sent, status: ${r.status}`);}catch(e){console.log('⚠️ Keep-alive ping failed:',e.message);}},4*60*1000);
